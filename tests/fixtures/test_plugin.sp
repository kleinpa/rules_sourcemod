// Fixture for rules_sourcemod analysis tests. The tests assert on providers, but
// building this target really does run spcomp, so it has to compile for real.
#include <sourcemod>

public void OnPluginStart() {}
